"""
Flask extensions for Peewee
Generated by RDT v2.0
"""
from flask_restx import Api
from flask_peewee.db import Database
{% if auth_enabled -%}
from flask_jwt_extended import JWTManager
{% endif -%}

# Initialize extensions
api = Api(
    title='{{ name | title_case }} API',
    version='1.0',
    description='API generated by RDT with Peewee',
    doc='/'  # Swagger documentation at root
)

# Peewee Database wrapper
db = Database()

{% if auth_enabled -%}
jwt = JWTManager()
{% endif -%}